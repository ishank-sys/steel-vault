version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: steelvault_dev_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: steelvault_dev
    ports:
      - "5433:5432" # changed host port to 5433 to avoid local postgres conflicts
    volumes:
      - db_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4:7
    container_name: steelvault_pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: pgadmin@localhost
      PGADMIN_DEFAULT_PASSWORD: pgadmin
    ports:
      - "8080:80"
    depends_on:
      - db

volumes:
  db_data:
